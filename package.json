
{
  "name": "seco-service-portal",
  "version": "1.0.0",
  "private": true,
  "description": "SECO Service Portal - static site served by Node/Express",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo \"No build step for static assets\"",
    "test": "echo \"No tests\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
